import { createStyles, makeStyles } from '@material-ui/core/styles'

export const useTabStyles = makeStyles(() =>
    createStyles({
        root: {
            backgroundColor: 'darblue',
        }
    })
)